{
  "name": "mtsn2kolut-pusaka-worker",
  "version": "1.0.0",
  "description": "PUSAKA attendance worker — long-running job consumer dengan HTTP health server (kompatibel Passenger/Paas).",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc --noEmit",
    "test": "NODE_ENV=test node --import tsx --test src/**/*.test.ts",
    "postinstall": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@types/node": "^25.6.0",
    "dotenv": "^17.4.2",
    "playwright": "^1.59.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "devDependencies": {}
}

// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: parents.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createParent = `-- name: CreateParent :one
INSERT INTO parents (nama, phone, address)
VALUES ($1, $2, $3)
RETURNING id, nama, phone, address, created_at, updated_at, photo_url
`

type CreateParentParams struct {
	Nama    string `json:"nama"`
	Phone   string `json:"phone"`
	Address string `json:"address"`
}

func (q *Queries) CreateParent(ctx context.Context, arg CreateParentParams) (Parent, error) {
	row := q.db.QueryRow(ctx, createParent, arg.Nama, arg.Phone, arg.Address)
	var i Parent
	err := row.Scan(
		&i.ID,
		&i.Nama,
		&i.Phone,
		&i.Address,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.PhotoUrl,
	)
	return i, err
}

const deleteParent = `-- name: DeleteParent :exec
DELETE FROM parents WHERE id = $1
`

func (q *Queries) DeleteParent(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteParent, id)
	return err
}

const getParent = `-- name: GetParent :one
SELECT id, nama, phone, address, created_at, updated_at, photo_url
FROM parents
WHERE id = $1
`

func (q *Queries) GetParent(ctx context.Context, id pgtype.UUID) (Parent, error) {
	row := q.db.QueryRow(ctx, getParent, id)
	var i Parent
	err := row.Scan(
		&i.ID,
		&i.Nama,
		&i.Phone,
		&i.Address,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.PhotoUrl,
	)
	return i, err
}

const linkParentStudent = `-- name: LinkParentStudent :exec
INSERT INTO parent_students (parent_id, student_id)
VALUES ($1, $2)
ON CONFLICT DO NOTHING
`

type LinkParentStudentParams struct {
	ParentID  pgtype.UUID `json:"parent_id"`
	StudentID pgtype.UUID `json:"student_id"`
}

func (q *Queries) LinkParentStudent(ctx context.Context, arg LinkParentStudentParams) error {
	_, err := q.db.Exec(ctx, linkParentStudent, arg.ParentID, arg.StudentID)
	return err
}

const listParentChildren = `-- name: ListParentChildren :many
SELECT s.id, s.nis, s.nama, s.class_id, c.name as class_name
FROM students s
JOIN parent_students ps ON ps.student_id = s.id
LEFT JOIN school_classes c ON c.id = s.class_id
WHERE ps.parent_id = $1
`

type ListParentChildrenRow struct {
	ID        pgtype.UUID `json:"id"`
	Nis       string      `json:"nis"`
	Nama      string      `json:"nama"`
	ClassID   pgtype.UUID `json:"class_id"`
	ClassName pgtype.Text `json:"class_name"`
}

func (q *Queries) ListParentChildren(ctx context.Context, parentID pgtype.UUID) ([]ListParentChildrenRow, error) {
	rows, err := q.db.Query(ctx, listParentChildren, parentID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListParentChildrenRow{}
	for rows.Next() {
		var i ListParentChildrenRow
		if err := rows.Scan(
			&i.ID,
			&i.Nis,
			&i.Nama,
			&i.ClassID,
			&i.ClassName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listParents = `-- name: ListParents :many
SELECT id, nama, phone, address, created_at, updated_at, photo_url
FROM parents
ORDER BY nama ASC
`

func (q *Queries) ListParents(ctx context.Context) ([]Parent, error) {
	rows, err := q.db.Query(ctx, listParents)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Parent{}
	for rows.Next() {
		var i Parent
		if err := rows.Scan(
			&i.ID,
			&i.Nama,
			&i.Phone,
			&i.Address,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.PhotoUrl,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listStudentParents = `-- name: ListStudentParents :many
SELECT p.id, p.nama, p.phone
FROM parents p
JOIN parent_students ps ON ps.parent_id = p.id
WHERE ps.student_id = $1
`

type ListStudentParentsRow struct {
	ID    pgtype.UUID `json:"id"`
	Nama  string      `json:"nama"`
	Phone string      `json:"phone"`
}

func (q *Queries) ListStudentParents(ctx context.Context, studentID pgtype.UUID) ([]ListStudentParentsRow, error) {
	rows, err := q.db.Query(ctx, listStudentParents, studentID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListStudentParentsRow{}
	for rows.Next() {
		var i ListStudentParentsRow
		if err := rows.Scan(&i.ID, &i.Nama, &i.Phone); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const unlinkParentStudent = `-- name: UnlinkParentStudent :exec
DELETE FROM parent_students
WHERE parent_id = $1 AND student_id = $2
`

type UnlinkParentStudentParams struct {
	ParentID  pgtype.UUID `json:"parent_id"`
	StudentID pgtype.UUID `json:"student_id"`
}

func (q *Queries) UnlinkParentStudent(ctx context.Context, arg UnlinkParentStudentParams) error {
	_, err := q.db.Exec(ctx, unlinkParentStudent, arg.ParentID, arg.StudentID)
	return err
}

const updateParent = `-- name: UpdateParent :one
UPDATE parents
SET nama = $2, phone = $3, address = $4, updated_at = NOW()
WHERE id = $1
RETURNING id, nama, phone, address, created_at, updated_at, photo_url
`

type UpdateParentParams struct {
	ID      pgtype.UUID `json:"id"`
	Nama    string      `json:"nama"`
	Phone   string      `json:"phone"`
	Address string      `json:"address"`
}

func (q *Queries) UpdateParent(ctx context.Context, arg UpdateParentParams) (Parent, error) {
	row := q.db.QueryRow(ctx, updateParent,
		arg.ID,
		arg.Nama,
		arg.Phone,
		arg.Address,
	)
	var i Parent
	err := row.Scan(
		&i.ID,
		&i.Nama,
		&i.Phone,
		&i.Address,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.PhotoUrl,
	)
	return i, err
}

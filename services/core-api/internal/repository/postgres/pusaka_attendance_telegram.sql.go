// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: pusaka_attendance_telegram.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createPusakaAttendanceTelegramLog = `-- name: CreatePusakaAttendanceTelegramLog :one
INSERT INTO pusaka_attendance_telegram_logs (
  report_date, target_chat_id, send_mode, schedule_time, status,
  telegram_message_id, error_message, requested_by
)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, report_date, target_chat_id, send_mode, schedule_time, status,
          telegram_message_id, error_message, requested_by, sent_at
`

type CreatePusakaAttendanceTelegramLogParams struct {
	ReportDate        pgtype.Date `json:"report_date"`
	TargetChatID      string      `json:"target_chat_id"`
	SendMode          string      `json:"send_mode"`
	ScheduleTime      string      `json:"schedule_time"`
	Status            string      `json:"status"`
	TelegramMessageID pgtype.Text `json:"telegram_message_id"`
	ErrorMessage      pgtype.Text `json:"error_message"`
	RequestedBy       pgtype.UUID `json:"requested_by"`
}

type CreatePusakaAttendanceTelegramLogRow struct {
	ID                pgtype.UUID        `json:"id"`
	ReportDate        pgtype.Date        `json:"report_date"`
	TargetChatID      string             `json:"target_chat_id"`
	SendMode          string             `json:"send_mode"`
	ScheduleTime      string             `json:"schedule_time"`
	Status            string             `json:"status"`
	TelegramMessageID pgtype.Text        `json:"telegram_message_id"`
	ErrorMessage      pgtype.Text        `json:"error_message"`
	RequestedBy       pgtype.UUID        `json:"requested_by"`
	SentAt            pgtype.Timestamptz `json:"sent_at"`
}

func (q *Queries) CreatePusakaAttendanceTelegramLog(ctx context.Context, arg CreatePusakaAttendanceTelegramLogParams) (CreatePusakaAttendanceTelegramLogRow, error) {
	row := q.db.QueryRow(ctx, createPusakaAttendanceTelegramLog,
		arg.ReportDate,
		arg.TargetChatID,
		arg.SendMode,
		arg.ScheduleTime,
		arg.Status,
		arg.TelegramMessageID,
		arg.ErrorMessage,
		arg.RequestedBy,
	)
	var i CreatePusakaAttendanceTelegramLogRow
	err := row.Scan(
		&i.ID,
		&i.ReportDate,
		&i.TargetChatID,
		&i.SendMode,
		&i.ScheduleTime,
		&i.Status,
		&i.TelegramMessageID,
		&i.ErrorMessage,
		&i.RequestedBy,
		&i.SentAt,
	)
	return i, err
}

const getPusakaAttendanceTelegramSettings = `-- name: GetPusakaAttendanceTelegramSettings :one
SELECT id, settings_key, is_enabled, send_time, timezone, target_chat_id,
       send_times, send_days, include_caption, include_image, report_mode, created_at, updated_at
FROM pusaka_attendance_telegram_settings
WHERE settings_key = 'default'
`

type GetPusakaAttendanceTelegramSettingsRow struct {
	ID             pgtype.UUID        `json:"id"`
	SettingsKey    string             `json:"settings_key"`
	IsEnabled      bool               `json:"is_enabled"`
	SendTime       pgtype.Time        `json:"send_time"`
	Timezone       string             `json:"timezone"`
	TargetChatID   string             `json:"target_chat_id"`
	SendTimes      []string           `json:"send_times"`
	SendDays       []int32            `json:"send_days"`
	IncludeCaption bool               `json:"include_caption"`
	IncludeImage   bool               `json:"include_image"`
	ReportMode     string             `json:"report_mode"`
	CreatedAt      pgtype.Timestamptz `json:"created_at"`
	UpdatedAt      pgtype.Timestamptz `json:"updated_at"`
}

func (q *Queries) GetPusakaAttendanceTelegramSettings(ctx context.Context) (GetPusakaAttendanceTelegramSettingsRow, error) {
	row := q.db.QueryRow(ctx, getPusakaAttendanceTelegramSettings)
	var i GetPusakaAttendanceTelegramSettingsRow
	err := row.Scan(
		&i.ID,
		&i.SettingsKey,
		&i.IsEnabled,
		&i.SendTime,
		&i.Timezone,
		&i.TargetChatID,
		&i.SendTimes,
		&i.SendDays,
		&i.IncludeCaption,
		&i.IncludeImage,
		&i.ReportMode,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const hasPusakaAttendanceTelegramScheduledLog = `-- name: HasPusakaAttendanceTelegramScheduledLog :one
SELECT EXISTS (
  SELECT 1
  FROM pusaka_attendance_telegram_logs
  WHERE report_date = $1
    AND target_chat_id = $2
    AND schedule_time = $3
    AND send_mode = 'scheduled'
)::boolean
`

type HasPusakaAttendanceTelegramScheduledLogParams struct {
	ReportDate   pgtype.Date `json:"report_date"`
	TargetChatID string      `json:"target_chat_id"`
	ScheduleTime string      `json:"schedule_time"`
}

func (q *Queries) HasPusakaAttendanceTelegramScheduledLog(ctx context.Context, arg HasPusakaAttendanceTelegramScheduledLogParams) (bool, error) {
	row := q.db.QueryRow(ctx, hasPusakaAttendanceTelegramScheduledLog, arg.ReportDate, arg.TargetChatID, arg.ScheduleTime)
	var column_1 bool
	err := row.Scan(&column_1)
	return column_1, err
}

const listPusakaAttendanceTelegramLogs = `-- name: ListPusakaAttendanceTelegramLogs :many
SELECT id,
       report_date,
       CASE
         WHEN length(target_chat_id) <= 4 THEN repeat('*', length(target_chat_id))
         ELSE repeat('*', GREATEST(length(target_chat_id) - 4, 0)) || right(target_chat_id, 4)
       END::text AS target_chat_id_masked,
       send_mode,
       schedule_time,
       status,
       telegram_message_id,
       error_message,
       requested_by,
       sent_at
FROM pusaka_attendance_telegram_logs
ORDER BY sent_at DESC
LIMIT $1 OFFSET $2
`

type ListPusakaAttendanceTelegramLogsParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

type ListPusakaAttendanceTelegramLogsRow struct {
	ID                 pgtype.UUID        `json:"id"`
	ReportDate         pgtype.Date        `json:"report_date"`
	TargetChatIDMasked string             `json:"target_chat_id_masked"`
	SendMode           string             `json:"send_mode"`
	ScheduleTime       string             `json:"schedule_time"`
	Status             string             `json:"status"`
	TelegramMessageID  pgtype.Text        `json:"telegram_message_id"`
	ErrorMessage       pgtype.Text        `json:"error_message"`
	RequestedBy        pgtype.UUID        `json:"requested_by"`
	SentAt             pgtype.Timestamptz `json:"sent_at"`
}

func (q *Queries) ListPusakaAttendanceTelegramLogs(ctx context.Context, arg ListPusakaAttendanceTelegramLogsParams) ([]ListPusakaAttendanceTelegramLogsRow, error) {
	rows, err := q.db.Query(ctx, listPusakaAttendanceTelegramLogs, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPusakaAttendanceTelegramLogsRow{}
	for rows.Next() {
		var i ListPusakaAttendanceTelegramLogsRow
		if err := rows.Scan(
			&i.ID,
			&i.ReportDate,
			&i.TargetChatIDMasked,
			&i.SendMode,
			&i.ScheduleTime,
			&i.Status,
			&i.TelegramMessageID,
			&i.ErrorMessage,
			&i.RequestedBy,
			&i.SentAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPusakaAttendanceTelegramReportRows = `-- name: ListPusakaAttendanceTelegramReportRows :many
SELECT e.id AS employee_id,
       e.nama AS employee_nama,
       COALESCE(e.nip, '')::text AS employee_nip,
       COALESCE(ar.jam_masuk, '')::text AS jam_masuk,
       COALESCE(ar.jam_pulang, '')::text AS jam_pulang
FROM employees e
LEFT JOIN attendance_records ar
  ON ar.employee_id = e.id
 AND ar.tanggal = $1
WHERE e.is_active = TRUE
  AND e.employment_type IN ('pns', 'pppk')
  -- Akun dev/test/dummy tidak boleh ikut laporan resmi Telegram/image.
  AND upper(e.nama) NOT LIKE '%DEV%'
  AND upper(e.nama) NOT LIKE '%TEST%'
  AND upper(e.nama) NOT LIKE '%DUMMY%'
  AND upper(e.nama) NOT LIKE '%CBT%'
ORDER BY e.nama ASC
`

type ListPusakaAttendanceTelegramReportRowsRow struct {
	EmployeeID   pgtype.UUID `json:"employee_id"`
	EmployeeNama string      `json:"employee_nama"`
	EmployeeNip  string      `json:"employee_nip"`
	JamMasuk     string      `json:"jam_masuk"`
	JamPulang    string      `json:"jam_pulang"`
}

func (q *Queries) ListPusakaAttendanceTelegramReportRows(ctx context.Context, tanggal pgtype.Date) ([]ListPusakaAttendanceTelegramReportRowsRow, error) {
	rows, err := q.db.Query(ctx, listPusakaAttendanceTelegramReportRows, tanggal)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPusakaAttendanceTelegramReportRowsRow{}
	for rows.Next() {
		var i ListPusakaAttendanceTelegramReportRowsRow
		if err := rows.Scan(
			&i.EmployeeID,
			&i.EmployeeNama,
			&i.EmployeeNip,
			&i.JamMasuk,
			&i.JamPulang,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertPusakaAttendanceTelegramSettings = `-- name: UpsertPusakaAttendanceTelegramSettings :one
INSERT INTO pusaka_attendance_telegram_settings (
  settings_key, is_enabled, send_time, send_times, send_days, timezone, target_chat_id,
  include_caption, include_image, report_mode
)
VALUES ('default', $1, $2, $3, $4, $5, $6, $7, $8, $9)
ON CONFLICT (settings_key) DO UPDATE
SET is_enabled = EXCLUDED.is_enabled,
    send_time = EXCLUDED.send_time,
    send_times = EXCLUDED.send_times,
    send_days = EXCLUDED.send_days,
    timezone = EXCLUDED.timezone,
    target_chat_id = EXCLUDED.target_chat_id,
    include_caption = EXCLUDED.include_caption,
    include_image = EXCLUDED.include_image,
    report_mode = EXCLUDED.report_mode,
    updated_at = now()
RETURNING id, settings_key, is_enabled, send_time, timezone, target_chat_id,
          send_times, send_days, include_caption, include_image, report_mode, created_at, updated_at
`

type UpsertPusakaAttendanceTelegramSettingsParams struct {
	IsEnabled      bool        `json:"is_enabled"`
	SendTime       pgtype.Time `json:"send_time"`
	SendTimes      []string    `json:"send_times"`
	SendDays       []int32     `json:"send_days"`
	Timezone       string      `json:"timezone"`
	TargetChatID   string      `json:"target_chat_id"`
	IncludeCaption bool        `json:"include_caption"`
	IncludeImage   bool        `json:"include_image"`
	ReportMode     string      `json:"report_mode"`
}

type UpsertPusakaAttendanceTelegramSettingsRow struct {
	ID             pgtype.UUID        `json:"id"`
	SettingsKey    string             `json:"settings_key"`
	IsEnabled      bool               `json:"is_enabled"`
	SendTime       pgtype.Time        `json:"send_time"`
	Timezone       string             `json:"timezone"`
	TargetChatID   string             `json:"target_chat_id"`
	SendTimes      []string           `json:"send_times"`
	SendDays       []int32            `json:"send_days"`
	IncludeCaption bool               `json:"include_caption"`
	IncludeImage   bool               `json:"include_image"`
	ReportMode     string             `json:"report_mode"`
	CreatedAt      pgtype.Timestamptz `json:"created_at"`
	UpdatedAt      pgtype.Timestamptz `json:"updated_at"`
}

func (q *Queries) UpsertPusakaAttendanceTelegramSettings(ctx context.Context, arg UpsertPusakaAttendanceTelegramSettingsParams) (UpsertPusakaAttendanceTelegramSettingsRow, error) {
	row := q.db.QueryRow(ctx, upsertPusakaAttendanceTelegramSettings,
		arg.IsEnabled,
		arg.SendTime,
		arg.SendTimes,
		arg.SendDays,
		arg.Timezone,
		arg.TargetChatID,
		arg.IncludeCaption,
		arg.IncludeImage,
		arg.ReportMode,
	)
	var i UpsertPusakaAttendanceTelegramSettingsRow
	err := row.Scan(
		&i.ID,
		&i.SettingsKey,
		&i.IsEnabled,
		&i.SendTime,
		&i.Timezone,
		&i.TargetChatID,
		&i.SendTimes,
		&i.SendDays,
		&i.IncludeCaption,
		&i.IncludeImage,
		&i.ReportMode,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
